import React from 'react';
import TopBar from '../global/topbar';
import { Box } from '@mui/material';

const Menu = () => {
    return (
        <Box sx={{ width: '100%' }}>
            <TopBar />
        </Box>
    );
};

export default Menu;